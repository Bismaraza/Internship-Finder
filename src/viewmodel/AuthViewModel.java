package viewmodel;

/**
 * Keeping this class for future extension (roles, sessions, hashing, etc).
 * The current console app handles login flows in UserViewModel + Main.
 */
public class AuthViewModel { }
